---
- name: Install and configure FreeIPA server
  hosts: ipaservers
  become: true
  vars_files:
    - ../vars/vault.yml
  
  roles:
    - role: freeipa.ansible_freeipa.ipaserver
      state: present
